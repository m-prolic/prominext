import type { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <h1 style={{ fontFamily: 'Arvo', fontWeight: 'bold' }}>
      hi there, THIS is only test for FONTS
    </h1>
  );
};

export default Home;
